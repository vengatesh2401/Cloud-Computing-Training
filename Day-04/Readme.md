# Day 4 AWS Training - Docker Installation and Web Application Deployment
Topics Covered
Amazon EC2 Instance Creation
Apache Installation
Docker Installation
Docker Service Configuration
Flask Application Setup
Dockerfile Creation
Docker Image Build
Docker Container Deployment
Screenshots
1. EC2 Instance Creation
File: day4-ec2-instance-created.png

This screenshot shows the creation of an Amazon EC2 instance, which is used as the server for deploying the web application.

2. Apache Installation
File: day4-apache-installation.png

This screenshot shows the installation of the Apache web server on the EC2 instance.

3. Docker Installation
File: day4-docker-installation.png

This screenshot shows the successful installation of Docker on the EC2 instance.

4. Docker Service
File: day4-docker-service.png

This screenshot shows that the Docker service is active and running. It also includes the creation of the Flask application file (app.py).

5. Docker Build
File: day4-docker-build.png

This screenshot shows the Dockerfile and the process of building the Docker image for the Flask web application.

6. Docker Container
File: day4-docker-container.png

This screenshot shows the Docker image, the running Docker container, and the published port (5000) used to access the application.

Learning Outcomes
Created an Amazon EC2 instance.
Installed Apache and Docker on the EC2 server.
Verified that the Docker service was running.
Created a Flask web application.
Wrote a Dockerfile for containerization.
Built a Docker image from the application.
Deployed the application inside a Docker container.
Learned the basics of deploying web applications using Docker on AWS EC2.
